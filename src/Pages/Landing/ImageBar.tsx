import { Image } from 'antd';
import Content from "../../Img/land-content.svg";
const ImageBar = () => {
    return (
        <Image src={Content} preview={false} />
    );
}

export default ImageBar;
